# Node.js
node_modules/
backend/uploads/
backend/database.sqlite
backend/database.sqlite-journal

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
env/

# React Vite
frontend/dist/
frontend/node_modules/
frontend/.eslintcache

# Env
.env

# OS
.DS_Store
Thumbs.db

# Data artifacts (large / derived — rebuild with scripts/build_index.py)
data/raw/
data/artifacts/
data/processed/*.csv.gz

# Python tooling
.pytest_cache/
.ruff_cache/
*.egg-info/
build/
dist/

# DIP stage dumps
out/
stages/

# Collected evaluation imagery — local only, not redistributed
data/raw/photos/
data/raw/packaging/
data/raw/gimages/
data/raw/web/
data/processed/generic.csv.superseded-*

# Derived from the DDInter release in data/raw/ddinter/, not authored here.
# Rebuilt by `python scripts/ingest_interactions.py`, exactly like the index in
# data/artifacts/. Kept out of git so the repository does not redistribute a
# third-party dataset, and so a 6 MB generated file does not live in history.
data/processed/interactions/
